# rag-core — RAG Primitives Library

RAG extraction project from DominicBE into a reusable Python package.

## Project Location

This repo lives at `C:\Users\Admin\Documents\DominicChatbot\rag-core`.
The Python package `rag_core` is under `src/rag_core/`.

## Phase 2 — Parsing and Chunking (IN PROGRESS)

Completed tasks:
- P01-T01: Package structure (Phase 1 skeleton) — migrated into correct repo
- P01-T02: RagCoreConfig dataclass — migrated into correct repo
- P01-T03: Core exceptions — migrated into correct repo
- P02-T01: Text normalization — extracted from DominicBE
- P02-T02: File extraction — extracted from DominicBE
- P02-T03: Chunking logic — extracted from DominicBE
- P02-T04: Ingestion pipeline protocol and implementations — extracted from DominicBE

Next: Phase 3 — Extract Embedding Layer

## Path Correction

The package was originally partially created in `C:\Users\Admin\Documents\DominicChatbot\rag_core`
(incorrect sibling). All useful artifacts have been migrated to the correct repo at
`C:\Users\Admin\Documents\DominicChatbot\rag-core\src\rag_core\`.
